# Optional -- only needed if the backend isn't reachable at the default
# http://localhost:5050 (e.g. running it on a different port/host).
# The Google Maps API key is NOT set here anymore -- it's entered through
# the app itself and served by the backend at runtime (see README.md's
# "Map" section / backend/server.py's /api/config).
VITE_BACKEND_URL=
